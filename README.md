# MyMoney

Приложение для управления личными и семейными финансами. Android MVP, iOS — на горизонте.

## Стек

**Backend:** Kotlin + Ktor + PostgreSQL + Exposed + Koin + JWT, Clean Architecture, миграции Flyway.
**Mobile:** Flutter + Riverpod + Isar + Dio, Clean Architecture, Offline First.
**Валюта на MVP:** только RUB, суммы хранятся как целые копейки (`BIGINT` / `Int64`).

## Структура

```
docs/                — документация, ADR, API-контракты
  MyMoney_Project_Bible_v2.md   ← источник истины (см. ниже)
  adr/               — Architecture Decision Records
  api/               — OpenAPI, контракты синхронизации
backend/             — Ktor-сервер, Clean Architecture
mobile/              — Flutter-клиент, Clean Architecture + Offline First
scripts/             — вспомогательные скрипты
docker-compose.yml   — локальный Postgres для разработки (будет добавлен на шаге 0.5)
```

Полная структура и стек — разделы 10–13 Bible v2.

## Источник истины

Все продуктовые, архитектурные и технические решения зафиксированы в **`docs/MyMoney_Project_Bible_v2.md`** (v2.0, объединяет предыдущие Bible v1.1 и Biblia v0.1; все конфликты разрешены журналом решений §20). Перед любым архитектурным решением сверяться с Bible и §4 (принцип принятия решений).

Актуальный технологический стек — **§11 Bible v2**; модель данных и DDL — **§§12–13**.

## Философия (кратко)

Приоритеты, в порядке важности: **Простота → Скорость → Надёжность → Безопасность → Понятность.**
Offline First — архитектурный контракт, не опция. Локальная БД — источник истины на устройстве. Цель — добавление операции ≤ 3 шагов, < 10 сек (см. UC-03/UC-04 Bible).

## Definition of Done (для пользовательской истории)

История считается завершённой только когда реализованы **все пять** пунктов:
1. UI
2. Бизнес-логика
3. Локальное хранение
4. Синхронизация
5. Тестирование

Подробнее — §16 Bible v2.

## Как запустить и тестировать

**Полное практическое руководство — [`HOWTO_RUN.md`](HOWTO_RUN.md).**
Там разложено: что установить на ПК, как поднять бэкенд, как собрать APK,
как подключить Android-смартфон и как вручную проверить приёмочные
сценарии Этапа 1 (в т.ч. ключевой DoD «добавить операцию за ≤ 3 шага»).

Короткая версия:

```powershell
# Backend (по желанию — мобильное приложение работает offline-first)
Copy-Item .env.example .env
docker compose up -d postgres
cd backend
.\gradlew.bat run

# Mobile
cd mobile
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter run                # на подключенный телефон / эмулятор
```

## Roadmap (кратко)

- Этап 0 — фундамент (структура, ADR, схема БД, скелеты Ktor и Flutter) ✅
- Этап 1 — ядро учёта (счета, категории, операции + audit trail) ✅
- Этап 2 — планирование (бюджет, цели) ✅
- Этап 3 — аналитика + PDF ✅
- Этап 4 — синхронизация + бэкап как снапшот sync ✅ *(семейный бюджет исключён из MVP — §21 Bible v2)*
- Этап 5 — долги (с процентами и графиком платежей) + подписки 🚀 **сейчас здесь**
- Этап 6 — полировка MVP

Полная roadmap — §17 Bible v2.
